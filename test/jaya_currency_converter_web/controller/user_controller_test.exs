defmodule JayaCurrencyConverterWeb.UserControllerTest do
  use JayaCurrencyConverterWeb.ConnCase

#   describe "transactions" do
#     @valid_user %{
#       name: "Frank"
#     }

#     test "POST create an user /", %{conn: conn} do
#       conn =
#         conn
#         |> post(Routes.user_path(conn, :create), user: @valid_user)

#       assert %{id: id} = redirected_params(conn)
#       assert redirected_to(conn) == Routes.user_path(conn, :show, id)

#     #   conn = get(conn, Routes.user_path(conn, :show, id))
#     end

    # test "GET all users /", %{conn: conn} do
    # end

    # test "GET show a user /", %{conn: conn} do
    #   user = JayaCurrencyConverter.Accounts.get_user()
    # end
#   end
end
